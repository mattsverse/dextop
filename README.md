# Dextop

A desktop application to read and display [dex](https://dex.rip/) tasks for a project.

## Installation

You can find the latest release [here](https://github.com/mattsverse/dextop/releases/latest/)

### Build your own

- clone the repository
- install dependencies with `bun install`
- run the build command with `bun tauri build`
